#include "cards.h"

float preflop_equity(Hand draw, int n_villains, int n_samples = 1000){
	int wins = 0;
	int losses = 0;
	for(int i = 0; i != n_samples, i++){
		Hand full_draw
		std::vector<Hand> villain_draws = random_table(n_villains, draw);
		
	}
} 

int main(){
	srand(time(NULL));
	std::cout << rand() % 10;
}